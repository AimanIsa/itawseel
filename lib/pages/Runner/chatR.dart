import 'package:flutter/material.dart';

class ChatR extends StatelessWidget {
  const ChatR({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("This is runner chat")],
      ),
    );
  }
}
